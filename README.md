# QuizHub MVP (Vue + Node.js + Socket.IO)

## Выбранный стек

- Frontend: `Vue 3`, `Vite`, `Pinia`, `Vue Router`, `Socket.IO Client`.
- Backend: `Node.js`, `Express`, `Socket.IO`.
- База данных: `SQLite` через `better-sqlite3`.
- Авторизация: `JWT` + `bcryptjs`.
- Браузерная сессия: подписанный JWT в `HttpOnly SameSite` cookie.
- Контейнеризация: `Docker Compose`, nginx proxy для frontend/API/Socket.IO.

## Что реализовано

- Регистрация и авторизация пользователей.
- Роли: `organizer` и `participant`.
- Конструктор квизов:
  - одиночный и множественный выбор;
  - вопросы с изображением по URL;
  - загрузка PNG, JPEG и WebP до 5 МБ с предпросмотром;
  - таймер и баллы на вопрос;
  - выбор категории из подготовленного списка;
  - гибкие настройки проведения.
- Управление квизами:
  - редактирование;
  - дублирование;
  - удаление с подтверждением и запретом удаления активной комнаты.
- Live-сессия по коду комнаты:
  - экран ожидания участников до старта;
  - старт, следующий вопрос, завершение;
  - доступ к ответу только во время активного вопроса;
  - обратный отсчет перед показом вопроса;
  - счетчик ответивших и не ответивших игроков;
  - автоматическое закрытие вопроса по таймеру;
  - realtime-события через Socket.IO;
  - live leaderboard.
- Режим администратора:
  - сводка по сессии с KPI;
  - график точности по вопросам;
  - распределение выбранных вариантов ответа;
  - график прогресса участников;
  - результаты каждого участника по вопросам;
  - экспорт результатов в CSV.
- Личный кабинет и история:
  - организатор видит созданные квизы и проведенные сессии;
  - участник видит историю участий и очки.

## Настройки квиза

По результатам анализа Kahoot, Wayground/Quizizz и Mentimeter в MVP добавлены:

- перемешивание порядка вопросов;
- перемешивание вариантов ответов;
- показ/скрытие лидерборда после каждого вопроса;
- показ/скрытие правильного ответа после вопроса;
- разрешение/запрет позднего входа;
- разрешение/запрет изменения ответа;
- максимум попыток на вопрос;
- обратный отсчет перед вопросом: `0`, `3`, `5`, `10`, `15`, `30` секунд;
- режимы очков:
  - `speed` — точность + скорость;
  - `accuracy` — только точность;
  - `disabled` — без очков.

## Docker-запуск

```bash
cd /Users/dhelprat/Downloads/числаки/vk-quiz-mvp
docker compose up --build -d
```

Открыть:

- приложение: [http://127.0.0.1:8080](http://127.0.0.1:8080)
- API health: [http://127.0.0.1:8080/api/health](http://127.0.0.1:8080/api/health)

В Docker backend не публикуется наружу и доступен только через nginx по пути `/api`.

Остановить:

```bash
docker compose down
```

Очистить БД Docker-volume:

```bash
docker compose down -v
```

## Локальный запуск без Docker

Backend:

```bash
cd /Users/dhelprat/Downloads/числаки/vk-quiz-mvp/backend
cp .env.example .env
npm install
npm start
```

Frontend:

```bash
cd /Users/dhelprat/Downloads/числаки/vk-quiz-mvp/frontend
cp .env.example .env
npm install
npm run dev
```

Frontend dev-сервер: [http://127.0.0.1:5173](http://127.0.0.1:5173)

## Тестирование

Backend:

```bash
cd /Users/dhelprat/Downloads/числаки/vk-quiz-mvp/backend
npm run smoke
npm run qa
npm audit
```

Frontend:

```bash
cd /Users/dhelprat/Downloads/числаки/vk-quiz-mvp/frontend
npm run build
npm run e2e
npm audit
```

`npm run qa` проверяет расширенные backend/API/WebSocket-сценарии: валидацию регистрации, роли, права доступа к квизам, аналитике и экспорту, запрет чтения правильных ответов участником, подключение по коду, экран ожидания, live-сессию из нескольких вопросов, прогресс ответивших, обратный отсчет, поздний вход, пустые и неверные ответы, запрет изменения ответа, лимит попыток, пересчет очков, историю и CORS.

`npm run e2e` самостоятельно запускает изолированный backend на порту `8010` и frontend и проходит шесть Chromium-сценариев: полный desktop-flow, дизайн-галерею, CRUD/настройки организатора, скрытие live-лидерборда, публичные страницы на ширинах `360` и `390` пикселей и полный мобильный flow. Мобильные проверки контролируют горизонтальное переполнение документа, выход интерактивных элементов за viewport и переполнение содержимого карточек. Скриншоты сохраняются в `docs/qa-screenshots` и `docs/mobile-qa`.

## Безопасность

- пароли хранятся как `bcrypt`-хэши;
- браузер не хранит JWT в `localStorage`;
- сессия передается в `HttpOnly`, `SameSite=Lax` cookie;
- Bearer JWT оставлен для автоматизированных API-тестов и внешних клиентов;
- вход и регистрация защищены rate limiting;
- REST API, WebSocket, аналитика, экспорт и CRUD проверяют роль и владельца;
- CSP устанавливается backend и nginx;
- правильные ответы не попадают в публичную модель вопроса;
- production backend требует `SECRET_KEY` длиной не менее 32 символов;
- backend-контейнер не публикует порт наружу.
